import { theme } from "antd"

export const themeAnt = {
    algorithm: theme.darkAlgorithm,
    token: {
        colorPrimary: "#008000"
    },
    components: {
        Statistic: {
            contentFontSize: 20,
            titleFontSize: 20
        }
    }
}
